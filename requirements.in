# Top-level dependencies for VANTA

# Core API & Server
fastapi[all]
uvicorn[standard]
pydantic

# LLM Serving
# vllm

# Config & Utils
pyyaml
python-dotenv
orjson

# LLM Integration
ollama
openai
requests

# Self-Mutation / Janus System
redis
rq # Redis Queue for task management
watchdog # For file system monitoring
GitPython # For programmatic Git interaction

# Optional Vector DB (uncomment if needed)
# chromadb 
qdrant-client # Added for Data Catalog persistence

# Other direct dependencies
transformers
tokenizers
# torch==2.6.0 # Pinned to match vllm==0.8.3 requirement
# outlines # Temporarily commented out to isolate dependency conflict
sentence-transformers # Added for knowledge embedding 

# aiortc==1.11.0
# numpy==2.2.5
# vllm==0.8.3 